spring:
  application:
    name: spring-boot
server:
  port: 8080

logging:
  level:
    root: ERROR
#You can control the logging level
#go to spring website and check appendix -> common application properties to check all the properties

